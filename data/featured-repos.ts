// Repos allowed to appear in the "Live from GitHub" section on the home
// page. Stats (stars, languages, counts) still aggregate over all repos;
// this only controls which repo cards are displayed.
export const featuredRepos = [
  "Cortex",
  "self-healing-system",
  "WindowShop_AI",
  "GENAI-Smart-Medical-Chatbot",
] as const;
